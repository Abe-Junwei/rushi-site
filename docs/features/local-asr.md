---
title: 本机 ASR
description: Rushi 默认使用本机语音模型完成转写。
---

# 本机 ASR

Rushi 桌面端会拉起本机 ASR sidecar。随包默认权重完成首启 seed 后，可在断网状态下进行本机转写。

Windows 用户有 NVIDIA 显卡时，可在应用环境页下载 CUDA 侧车。GPU 组件不是首装主安装包，下载失败时仍可继续使用 CPU 转写。
